@page
@model SchoolLMS.Pages.AttendanceModel
@section Styles {
    <link rel="stylesheet" href="~/css/attendance.css"
    asp-append-version="true"
    />
}

@{
    ViewData["Title"] = "Attendance";
}

<div class="attendance-page">

    <!-- =========================================================
         ATTENDANCE HEADER
         ========================================================= -->

    <section class="attendance-hero">

        <div class="attendance-title-area">

            <!-- Future custom icon/logo can be placed here -->
            <div class="attendance-icon-placeholder" aria-hidden="true">
                📅
            </div>

            <div>
                <div class="attendance-eyebrow">STUDENT RECORD</div>
                <h1>Attendance</h1>

                <div class="attendance-class-info">
                    <span class="attendance-class-badge">
                        @Model.ClassName
                    </span>

                    <span class="attendance-section-badge">
                        @Model.SectionName
                    </span>
                </div>
            </div>

        </div>

        <div class="attendance-hero-summary">

            <div class="attendance-summary-icon">
                %
            </div>

            <div>
                <span>Overall Attendance</span>
                <strong>@Model.OverallAttendancePercentage.ToString("0.##")%</strong>
            </div>

        </div>

    </section>


    <!-- =========================================================
         CALENDAR CONTROLS
         ========================================================= -->

    <section class="attendance-calendar-card">

        <div class="calendar-toolbar">

            <button class="calendar-navigation-button"
                    type="button"
                    aria-label="Previous month">
                ‹
            </button>

            <div class="calendar-month">

                <div class="calendar-month-name">
                    @Model.CurrentMonthName
                </div>

                <div class="calendar-year">
                    @(Model.CalendarDays.FirstOrDefault(d => d.IsCurrentMonth)?.Date.Year)
                </div>

            </div>

            <button class="calendar-navigation-button"
                    type="button"
                    aria-label="Next month">
                ›
            </button>

            <button class="calendar-today-button"
                    type="button">
                Today
            </button>

        </div>


        <!-- =====================================================
             WEEKDAYS
             ===================================================== -->

        <div class="attendance-weekdays">

            @foreach (var day in Model.WeekDays)
            {
                <div>@day</div>
            }

        </div>


        <!-- =====================================================
             CALENDAR
             ===================================================== -->

        <div class="attendance-calendar">

            @foreach (var day in Model.CalendarDays)
            {
                <div class="attendance-day @day.CssClass @(day.IsToday ? "today" : "")">

                    @if (day.IsToday)
                    {
                        <span class="today-label">TODAY</span>
                    }

                    <span class="day-number">@day.DayNumber</span>

                    @if (day.IsCurrentMonth)
                    {
                        <span class="attendance-status-icon">@day.StatusIcon</span>
                        <span class="attendance-status-text">@day.StatusLabel</span>
                    }

                </div>
            }

        </div>


        <!-- =====================================================
             LEGEND
             ===================================================== -->

        <div class="attendance-legend">

            <div class="legend-item">
                <span class="legend-icon present">✓</span>
                Present
            </div>

            <div class="legend-item">
                <span class="legend-icon absent">×</span>
                Absent
            </div>

            <div class="legend-item">
                <span class="legend-icon holiday">OFF</span>
                Holiday / Weekend
            </div>

            <div class="legend-item">
                <span class="legend-icon pending">—</span>
                Not Recorded
            </div>

        </div>

    </section>


    <!-- =========================================================
         TODAY'S ATTENDANCE
         ========================================================= -->

    @if (Model.Today is not null)
    {
        <section class="attendance-today-card">

            <div class="today-card-icon">
                @Model.Today.StatusIcon
            </div>

            <div class="today-card-content">

                <span class="today-card-label">
                    TODAY'S ATTENDANCE
                </span>

                <h2>
                    @Model.Today.Date.ToString("d MMMM yyyy")
                </h2>

                <p>
                    @Model.Today.Date.ToString("dddd")
                </p>

            </div>

            <div class="today-card-status">
                <span>@(Model.Today.StatusLabel ?? "Not Recorded")</span>
                <strong>@Model.Today.StatusIcon</strong>
            </div>

        </section>
    }


    <!-- =========================================================
         MONTHLY SUMMARY
         ========================================================= -->

    <section class="attendance-summary-section">

        <div class="section-heading">

            <div>
                <span class="section-eyebrow">
                    ATTENDANCE REPORT
                </span>

                <h2>
                    @Model.CurrentMonthName Summary
                </h2>
            </div>

            <!-- Future custom report icon -->
            <div class="summary-title-icon">
                %
            </div>

        </div>


        <div class="attendance-summary-grid">

            <div class="attendance-summary-card present-summary">

                <div class="summary-card-icon">
                    ✓
                </div>

                <div>
                    <span>Present</span>
                    <strong>@Model.MonthlyPresentCount</strong>
                    <small>Days</small>
                </div>

            </div>


            <div class="attendance-summary-card absent-summary">

                <div class="summary-card-icon">
                    ×
                </div>

                <div>
                    <span>Absent</span>
                    <strong>@Model.MonthlyAbsentCount</strong>
                    <small>Days</small>
                </div>

            </div>


            <div class="attendance-summary-card holiday-summary">

                <div class="summary-card-icon">
                    OFF
                </div>

                <div>
                    <span>Holidays</span>
                    <strong>@Model.MonthlyHolidayCount</strong>
                    <small>Days</small>
                </div>

            </div>


            <div class="attendance-summary-card pending-summary">

                <div class="summary-card-icon">
                    —
                </div>

                <div>
                    <span>Not Recorded</span>
                    <strong>@Model.MonthlyNotRecordedCount</strong>
                    <small>Day@(Model.MonthlyNotRecordedCount == 1 ? "" : "s")</small>
                </div>

            </div>

        </div>


        <!-- =====================================================
             ATTENDANCE PERCENTAGE
             ===================================================== -->

        <div class="attendance-percentage-card">

            <div class="percentage-heading">

                <div>
                    <span>MONTHLY ATTENDANCE</span>
                    <h3>Attendance Percentage</h3>
                </div>

                <strong>@Model.MonthlyAttendancePercentage.ToString("0.##")%</strong>

            </div>

            <div class="percentage-bar">

                <div class="percentage-fill"
                     style="width: @(Model.MonthlyAttendancePercentage.ToString("0.##"))%;">
                </div>

            </div>

            <div class="percentage-footer">

                <span>
                    Based on recorded working days
                </span>

                <span>
                    @Model.MonthlyPresentCount Present / @Model.MonthlyRecordedDays Recorded
                </span>

            </div>

        </div>

    </section>

</div>

@section Scripts {
    <script src="~/js/Attendance.js"
    asp-append-version="true"></script>
}
