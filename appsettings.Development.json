{
  "DetailedErrors": true,
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    // Local dev database, talking to the SQL Server Express instance
    // installed on this machine (default instance name from a winget/
    // standard install is SQLEXPRESS). Windows-authenticated, so no
    // password is stored here.
    "DefaultConnection": "Server=localhost\\SQLEXPRESS;Database=SchoolLMS_Dev;Trusted_Connection=True;TrustServerCertificate=True;"
  }
}
