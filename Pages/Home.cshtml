@page
@model SchoolLMS.Pages.HomeModel
@{
    ViewData["Title"] = "My Subjects";
}

@section Styles {
    <link rel="stylesheet" href="~/css/home.css" />
}

<!--
    NOTE: This page's subject-card UI is very similar to /class
    (Pages/class.cshtml), which already covers the same "subjects
    with teacher + options" concept inside the shared layout with a
    sidebar entry ("Class"). Consider merging the two, or wiring a
    nav link to whichever one becomes the single source of truth.
-->

<h2 class="section-heading">My Subjects</h2>

<div class="subjects-grid">

    @foreach (var subject in Model.Subjects)
    {
        <div class="subject-card">

            <h3 class="subject-name">
                @subject.Name.ToUpper()
            </h3>


            <!-- Teacher information -->
            <div class="teacher-info">

                <!-- Reserved space for teacher's future photo -->
                <div class="teacher-photo-placeholder"></div>

                <span class="teacher-name">
                    @subject.TeacherName
                </span>

            </div>


            <!-- Subject options -->
            <div class="subject-options">

                @foreach (var option in Model.SubjectOptions)
                {
                    <button type="button" class="option-item">

                        <!-- Reserved space for future icon -->
                        <span class="option-icon-placeholder"></span>

                        <span class="option-label">
                            @option
                        </span>

                    </button>
                }

            </div>

        </div>
    }

</div>
